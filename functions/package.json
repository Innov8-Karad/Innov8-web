{
  "name": "innov8-functions",
  "version": "1.0.0",
  "description": "Innov8 Firebase Cloud Functions — Auth, FCM Push, Cloudinary Delete",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "lint": "eslint src/**/*.ts"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "cloudinary": "^2.5.1",
    "@google/generative-ai": "^0.24.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.6.0",
    "nodemailer": "^8.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/nodemailer": "^8.0.0",
    "typescript": "~5.7.0"
  },
  "private": true
}
